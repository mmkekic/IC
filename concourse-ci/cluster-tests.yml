
platform: linux
image_resource:
  type: docker-image
  source: {repository: ubuntu, tag: 18.04}
inputs:
  - name: IC
run:
  path: bash
  args: [ "concourse-ci/cluster-tests.sh" ]
  dir: IC
params:
  FAKE_CLUSTER_SSH_PRIVATE_KEY: ((FAKE_CLUSTER_SSH_PRIVATE_KEY))
